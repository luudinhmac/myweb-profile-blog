# Description: Helm values for the frontend staging environment.
# Overrides default values with staging-specific settings and automated tag updates.
# Last Updated: 2026-05-12

image:
  tag: "dev-3a2c4735"

ingress:
  enabled: true
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
  hosts:
    - host: staging.luumac.io.vn
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: luumac-wildcard-tls
      hosts:
        - staging.luumac.io.vn

env:
  tz: "Asia/Ho_Chi_Minh"
  nodeEnv: "production"
  internalApiUrl: "http://portfolio-backend-staging:3001/api/v1"
