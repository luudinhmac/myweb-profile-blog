# Description: Helm values for the PostgreSQL staging database.
# Overrides default values with staging-specific storage and configuration settings.
# Last Updated: 2026-05-12

storage:
  size: 5Gi

service:
  type: ClusterIP
  port: 5432
