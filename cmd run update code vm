# 1. Nén và đồng bộ (Phiên bản hỗ trợ hiển thị ảnh)
tar --exclude='node_modules' --exclude='dist' --exclude='.next' --exclude='.git' -cvzf project.tar.gz ansible backend frontend package.json pnpm-lock.yaml .env
scp project.tar.gz macld@192.168.157.50:/data/Portfolio
ssh macld@192.168.157.50 "cd /data/Portfolio && tar -xvzf project.tar.gz && rm project.tar.gz"

# 2. Chạy Playbook để cập nhật cấu hình file vật lý
ssh macld@192.168.157.50 "cd /data/Portfolio/ansible && ansible-playbook -i inventory.ini playbooks/staging.yml"

# 3. BUỘC Nginx nhận cấu hình mới
ssh macld@192.168.157.109 "docker rm -f portfolio-nginx && cd /opt/portfolio && docker compose up -d nginx"
