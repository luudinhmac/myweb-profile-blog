You are a senior NestJS developer.

Task:
Build backend API.

Rules:
- controller → service → module
- enable CORS for localhost:3000
- use env variables
- Comment notifications are explicitly disabled per user request (Telegram, MS Teams, Email).


Port:
- run on 3001

Output:
- modules
- DTO
- routes