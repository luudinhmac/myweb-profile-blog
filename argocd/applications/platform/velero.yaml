apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-velero
  namespace: argocd
spec:
  project: platform-project
  source:
    chart: velero
    repoURL: https://vmware-tanzu.github.io/helm-charts
    targetRevision: 7.1.2
    helm:
      releaseName: velero
      values: |
        configuration:
          backupStorageLocation:
            - name: default
              provider: aws
              bucket: velero-k8s-prod
              config:
                region: auto
                s3ForcePathStyle: true
                s3Url: https://a8823974263cfbb734616640f1b9dd55.r2.cloudflarestorage.com
          # Default to using File System Backup (restic/kopia) for all PVs
          defaultVolumesToFsBackup: true
          extraEnvVars:
            AWS_EC2_METADATA_DISABLED: "true"
        
        # Deploy Node Agent (daemonset) to perform filesystem backup on PVs
        deployNodeAgent: true
        
        # Override kubectl image for CRD upgrades since bitnami/kubectl:1.31 does not exist
        kubectl:
          image:
            repository: rancher/kubectl
            tag: v1.31.1

        # Use existing secret managed by Ansible
        credentials:
          existingSecret: cloud-credentials
          secretKeys:
            cloud: cloud

        initContainers:
          - name: velero-plugin-for-aws
            image: velero/velero-plugin-for-aws:v1.10.1
            imagePullPolicy: IfNotPresent
            volumeMounts:
              - mountPath: /target
                name: plugins
  destination:
    server: https://kubernetes.default.svc
    namespace: velero
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
