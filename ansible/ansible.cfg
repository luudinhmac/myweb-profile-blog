# Description: Ansible configuration for the mature V2 architecture.
# Ensures roles are discovered correctly across directories.

[defaults]
inventory = inventory/staging.ini
roles_path = roles
host_key_checking = False
# stdout_callback = yaml
# bin_ansible_callbacks = True

[ssh_connection]
pipelining = True
