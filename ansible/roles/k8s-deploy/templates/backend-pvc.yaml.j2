apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ project_name }}-backend-pvc"
  namespace: "{{ k8s_namespace }}"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ backend_storage_size | default('5Gi') }}
  storageClassName: ""
